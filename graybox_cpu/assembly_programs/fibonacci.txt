; this program will continuously find numbers in the Fibonacci sequence
; and store them into RAM (it'll try, at least)

; nice demonstration of uses for "swp"

 mov #1 A
 mov #1 B
 mov #100 X

loop:
 add
 str

 mov B ACC
 mov ACC A
 swp
 mov ACC B

 inc X
 jmp #loop
